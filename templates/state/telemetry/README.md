# Telemetry

This directory stores per-run telemetry files (`run-{id}.json`).
These are generated automatically by the orchestrator after each workflow run and consumed by the retrospective agent for self-evolution analysis.
